---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div style="display:flex;gap:6px;margin-top:10px;flex-wrap:wrap">
  {tags.map(t => <span class="proj-tag">{t}</span>)}
</div>
